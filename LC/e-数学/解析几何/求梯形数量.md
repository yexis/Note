### Title 统计梯形个数

##### 1 emphasis

- 求梯形数量
- 求梯形个数



##### 2 key points

 

##### 3 thought

###### 思路

- 统计所有梯形的数量（存在两边平行的四边形），包括平行四边形
- 由于平行四边形存在两组平行边，所以会重复计算一次，去重，计算平行四边形的数量



##### 4 code

[3625.统计梯形的数目](https://leetcode.cn/problems/count-number-of-trapezoids-ii/submissions/647242131/)

```cpp
#include <iostream>
#include <vector>
#include <string.h>
#include <algorithm>
#include <numeric>
#include <set>
#include <array>
#include <cassert>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <iomanip>
#include <string>
#include <sstream>
#include <vector>
#include <queue>
#include <stack>
#include <list>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>
#include <algorithm>
#include <complex>
#include <cmath>
#include <numeric>
#include <bitset>
#include <functional>
#include <random>
#include <ctime>
#include <limits>
#include <climits>

using namespace std;
#define ios ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)
#define next_per next_permutation
#define call(x) (x).begin(), (x).end()
#define debug(x) cerr << (#x) << " = " << (x) << endl;
#define debugout(x) cout << (#x) << " = " << (x) << endl;
#define debugerr(x) cerr << (#x) << " = " << (x) << endl;

using ll = long long;
using ull = unsigned long long;
using pii = pair<int, int>;
using pli = pair<ll, int>;
using pil = pair<int, ll>;
using pll = pair<ll, ll>;
using pbi = pair<bool, int>;
using pib = pair<int, bool>;
using pis = pair<int, string>;
using psi = pair<string, int>;
using puu = pair<ull, ull>;
using pdd = pair<double, double>;
using arr = array<pii, 4>;
using arr3 = array<int, 3>;
using arr4 = array<int, 4>;
using arr5 = array<int, 5>;

const int dir[4][2] = {{-1, 0},
                       {1,  0},
                       {0,  -1},
                       {0,  1}};
const int INF = 0x3f3f3f3f;
const ll LLINF = 0x3f3f3f3f3f3f3f3f;
const int mod = 1e9 + 7;
const string YES = "YES";
const string NO = "NO";


// 计算直线(二元一次方程) 斜率k 和 截距b
// 使用pii(1, 0)表示斜率为0，平行于x轴，此时 b = y
// 使用pii(0, 1)表示斜率为正无穷，平行于y轴，此时 b = x
KB cal_kb(int x1, int y1, int x2, int y2) {
    KB kb; 
    auto& k = kb.k; 
    auto& b = kb.b;
    
    int dx = x2 - x1;
    int dy = y2 - y1;

    int g = gcd(dx, dy);
    dx /= g, dy /= g;
    if (dx < 0) dx = -dx, dy = -dy;

    k.first = dx, k.second = dy;

    if (k.first == 0) {
        // k 正无穷
        // k = pii(0, 1)
        k.second = 1;
        b.first = 1, b.second = x1;
    } else if (k.second == 0) {
        // k == 0
        // k = pii(1, 0)
        k.first = 1;
        b.first = 1, b.second = y1;
    } else {
        // b = y - (dy / dx) * x = y * dx - dy * x / dx
        b.first = dx, b.second = y1 * dx - dy * x1;
        g = gcd(b.first, b.second);
        b.first /= g, b.second /= g;
    }

    return kb;
}

// 统计平行四边形的个数
// 性质：平行四边形的两条对角线终点重合
// 所有对于中点重合的任意两条对角线，其一定能组成一个平行四边形
// 注意：如果两条对角线共线，那么无法组成平行四边形，需要排除
int cal_parallelogram(vector<vector<int>>& ps) {
    int n = ps.size();
    int ans = 0;
    // 中点在mid的斜率为k的线段个数
    map<pii, map<pii, int>> V;
    for (int i = 0; i < n; i++) {
        int x1 = ps[i][0], y1 = ps[i][1];
        for (int j = i + 1; j < n; j++) {
            int x2 = ps[j][0], y2 = ps[j][1];
            pii know = cal_kb(x1, y1, x2, y2).k;
            
            int xmid = (x1 + x2), ymid = (y1 + y2);
            pii mid = pii(xmid, ymid);
            
            for (auto& [k, v] : V[mid]) {
                if (k == know) continue;
                ans += v;
            }
            V[mid][know]++;
        }
    }
    return ans;
}

class Solution {
public:
    int countTrapezoids(vector<vector<int>>& ps) {
        int n = ps.size();

        // 普通，以与y轴交点为key
        map<pii, map<pii, int> > C;
        map<pii, int> sumC;

        for (int i = 0; i < n; i++) {
            int x1 = ps[i][0], y1 = ps[i][1];
            for (int j = i + 1; j < n; j++) {
                int x2 = ps[j][0], y2 = ps[j][1];
                auto kb = cal_kb(x1, y1, x2, y2);
                C[kb.k][kb.b]++;
            }
        }

        // 统计所有梯形的数量（存在两边平行的四边形）
        // 包括平行四边形
        int ans = 0;
        int t = 0;
        for (auto& [k, v] : C) {
            for (auto& [b, vv] : v) {
                sumC[k] += vv;
            } 
        }
        for (auto& [k, v] : C) {
            for (auto& [b, vv] : v) {
                t += vv * (sumC[k] - vv); 
            }
        }
        t /= 2; ans += t;

        // 由于平行四边形存在两组平行边
        // 所以会重复计算一次，去重
        // 计算平行四边形的数量
        int sub = cal_parallelogram(ps);
        return ans - sub;
    }
};

```



##### 5 summary

